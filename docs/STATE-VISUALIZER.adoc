= Patch Bridge State Visualizer

Current position as of 2026-03-30:

* the repository is a draft extraction target from `panic-attacker`
* publication-facing claims currently outrun the runnable artefacts
* template residue still exists in core release paths
* the crate manifest does not yet expose a runnable target

Current evidence:

* no real standalone crate target
* no real fuzz surface
* no in-repo end-to-end proof artefacts
* paper surface exists, but remains a draft under audit

Immediate blockers:

* false/template machine-readable state
* placeholder-heavy Justfile, container, and FFI surfaces
* fake fuzz placeholder
* formal-verification claims without checked supporting artefacts

Do not use this file as a progress dashboard for release readiness until the
underlying repository state has been made honest.
